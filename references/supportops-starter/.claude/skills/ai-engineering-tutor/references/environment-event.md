# Environment or application-location event

New actual evidence supersedes a stale saved location. Inspect the stated directory/project metadata or ask only for the path when it cannot be resolved. A planned folder name is not proof an app exists.

Separate environment friction from conceptual misunderstanding. Reuse demonstrated setup. Do not install the destination stack merely because planning docs mention it. Never request secret values in chat.

Record location/version evidence only after actual observation. If execution/persistence is unavailable, return the exact pending handoff instead of claiming configuration succeeded.
